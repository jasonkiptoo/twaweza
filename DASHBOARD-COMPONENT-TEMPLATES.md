# Dashboard & Contribution - Code Templates for Remaining Components

This file provides ready-to-use code templates for the remaining components that need to be implemented.

---

## 1. ContributionModal Component

**File:** `src/components/DashboardModals/ContributionModal.tsx`

```typescript
import { useCallback, useState } from "react";
import { Pressable, ScrollView, View } from "react-native";
import { AlertCircle, CheckCircle } from "lucide-react-native";
import { AppDialog } from "@/components/ui/dialog";
import { VStack } from "@/components/ui/vstack";
import { FormField } from "@/components/ui/FormField";
import { AppInput } from "@/components/ui/AppInput";
import { AppButton } from "@/components/ui/AppButton";
import { Text } from "@/components/ui/text";
import { useTheme } from "@/hooks/useTheme";
import { useAuthStore } from "@/store/authStore";
import { useContributionStore } from "@/store/contributionStore";
import { useContributionSettingsStore } from "@/store/contributionSettingsStore";
import { parseCurrency, formatCurrency } from "@/utils/currency";
import type { ContributionSettings } from "@/types/creditManagement";

interface ContributionModalProps {\n  open: boolean;\n  onClose: () => void;\n}\n\nexport function ContributionModal({ open, onClose }: ContributionModalProps) {\n  const { colors } = useTheme();\n  const token = useAuthStore((state) => state.token);\n  const add = useContributionStore((state) => state.add);\n  const mutating = useContributionStore((state) => state.mutating);\n  \n  const settings = useContributionSettingsStore((state) => state.settings);\n  const fetchSettings = useContributionSettingsStore((state) => state.fetch);\n  const settingsLoading = useContributionSettingsStore((state) => state.loading);\n\n  // Form state\n  const [amount, setAmount] = useState(\"\");\n  const [method, setMethod] = useState<\"Mpesa\" | \"Bank\" | \"cash\">(\"Mpesa\");\n  const [phone, setPhone] = useState(\"\");\n  const [reference, setReference] = useState(\"\");\n  const [error, setError] = useState(\"\");\n  const [success, setSuccess] = useState(\"\");\n\n  // Load settings when modal opens\n  useCallback(() => {\n    if (open && token && !settings) {\n      fetchSettings(token);\n    }\n  }, [open, token, settings, fetchSettings])();\n\n  async function handleSubmit() {\n    setError(\"\");\n    setSuccess(\"\");\n\n    // Validate\n    const numAmount = Number(amount);\n    if (!amount || numAmount <= 0) {\n      return setError(`Amount must be greater than 0`);\n    }\n\n    if (settings && numAmount < settings.minimumAmount) {\n      return setError(\n        `Minimum contribution is ${formatCurrency(settings.minimumAmount, settings.currency)}`\n      );\n    }\n\n    if (method === \"Mpesa\" && !phone.trim()) {\n      return setError(\"Phone number is required for M-Pesa\");\n    }\n\n    if (!token) {\n      return setError(\"Session expired. Please log in again.\");\n    }\n\n    try {\n      await add(token, {\n        amount: numAmount,\n        method,\n        phone: phone.trim() || undefined,\n        reference: reference.trim() || undefined,\n      });\n\n      setSuccess(\"Contribution submitted successfully!\");\n      \n      // Reset form\n      setAmount(\"\");\n      setPhone(\"\");\n      setReference(\"\");\n      setMethod(\"Mpesa\");\n\n      // Auto-close after delay\n      setTimeout(onClose, 1500);\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"Failed to submit contribution\");\n    }\n  }\n\n  function handleClose() {\n    setAmount(\"\");\n    setPhone(\"\");\n    setReference(\"\");\n    setError(\"\");\n    setSuccess(\"\");\n    setMethod(\"Mpesa\");\n    onClose();\n  }\n\n  if (!settings && !settingsLoading) {\n    return null;\n  }\n\n  if (!settings?.enabled) {\n    return (\n      <AppDialog\n        open={open}\n        title=\"Contributions Disabled\"\n        onClose={handleClose}\n      >\n        <VStack className=\"gap-3 p-4\">\n          <Text className=\"text-muted-foreground\">\n            Contributions are currently disabled for this group.\n          </Text>\n          <AppButton\n            title=\"Close\"\n            variant=\"outline\"\n            onPress={handleClose}\n          />\n        </VStack>\n      </AppDialog>\n    );\n  }\n\n  return (\n    <AppDialog\n      open={open}\n      title=\"💳 Add Contribution\"\n      subtitle={`Minimum: ${formatCurrency(settings?.minimumAmount || 0, settings?.currency || \"KES\")}`}\n      onClose={handleClose}\n      footer={\n        <View style={{ flexDirection: \"row\", gap: 12 }}>\n          <AppButton\n            title=\"Cancel\"\n            variant=\"outline\"\n            onPress={handleClose}\n            style={{ flex: 1 }}\n            disabled={mutating}\n          />\n          <AppButton\n            title={mutating ? \"Submitting...\" : \"Submit\"}\n            loading={mutating}\n            disabled={mutating}\n            onPress={handleSubmit}\n            style={{ flex: 1 }}\n          />\n        </View>\n      }\n    >\n      <ScrollView showsVerticalScrollIndicator={false}>\n        <VStack className=\"gap-4 p-4\">\n          {/* Amount */}\n          <FormField label=\"Amount\" required>\n            <AppInput\n              value={amount}\n              onChangeText={(text) => {\n                setAmount(text.replace(/\\D/g, \"\"));\n                setError(\"\");\n              }}\n              keyboardType=\"number-pad\"\n              placeholder={`Minimum: ${settings?.minimumAmount || 0}`}\n              editable={!mutating}\n            />\n          </FormField>\n\n          {/* Method */}\n          <FormField label=\"Payment Method\" required>\n            <View style={{ flexDirection: \"row\", gap: 8, flexWrap: \"wrap\" }}>\n              {(settings?.allowedMethods || [\"Mpesa\", \"Bank\", \"cash\"]).map(\n                (m) => (\n                  <AppButton\n                    key={m}\n                    title={m}\n                    variant={method === m ? \"default\" : \"outline\"}\n                    onPress={() => {\n                      setMethod(m as any);\n                      if (m !== \"Mpesa\") setPhone(\"\");\n                      setError(\"\");\n                    }}\n                    disabled={mutating}\n                  />\n                )\n              )}\n            </View>\n          </FormField>\n\n          {/* Phone (M-Pesa only) */}\n          {method === \"Mpesa\" && (\n            <FormField label=\"Phone Number\" required>\n              <AppInput\n                value={phone}\n                onChangeText={(text) => {\n                  setPhone(text);\n                  setError(\"\");\n                }}\n                keyboardType=\"phone-pad\"\n                placeholder=\"07XXXXXXXX\"\n                editable={!mutating}\n              />\n            </FormField>\n          )}\n\n          {/* Reference */}\n          <FormField label=\"Reference\" required={false}>\n            <AppInput\n              value={reference}\n              onChangeText={(text) => {\n                setReference(text);\n                setError(\"\");\n              }}\n              placeholder=\"Transaction reference\"\n              editable={!mutating}\n            />\n          </FormField>\n\n          {/* Help text */}\n          <View\n            style={{\n              backgroundColor: colors.muted + \"15\",\n              padding: 12,\n              borderRadius: 8,\n            }}\n          >\n            <Text className=\"text-xs text-muted-foreground\">\n              Frequency: {settings?.minimumFrequency || \"monthly\"}\\n\n              Required periods: {settings?.minimumPeriods || 1}\\n\n              Approval required: {settings?.approvalRequired ? \"Yes\" : \"No\"}\n            </Text>\n          </View>\n\n          {/* Errors */}\n          {error && (\n            <View\n              style={{\n                backgroundColor: colors.error + \"20\",\n                padding: 12,\n                borderRadius: 8,\n                flexDirection: \"row\",\n                gap: 8,\n              }}\n            >\n              <AlertCircle size={16} color={colors.error} />\n              <Text style={{ color: colors.error, flex: 1, fontSize: 13 }}>\n                {error}\n              </Text>\n            </View>\n          )}\n\n          {/* Success */}\n          {success && (\n            <View\n              style={{\n                backgroundColor: colors.success + \"20\",\n                padding: 12,\n                borderRadius: 8,\n                flexDirection: \"row\",\n                gap: 8,\n              }}\n            >\n              <CheckCircle size={16} color={colors.success} />\n              <Text style={{ color: colors.success, flex: 1, fontSize: 13 }}>\n                {success}\n              </Text>\n            </View>\n          )}\n        </VStack>\n      </ScrollView>\n    </AppDialog>\n  );\n}\n```

---

## 2. Contribution History Screen

**File:** `src/app/(tabs)/contributions.tsx`

```typescript\nimport { useFocusEffect } from \"@react-navigation/native\";\nimport { Plus, Search } from \"lucide-react-native\";\nimport { useCallback, useState } from \"react\";\nimport { FlatList, Pressable, RefreshControl, View } from \"react-native\";\nimport { Screen } from \"@/components/layout/Screen\";\nimport { AppCard } from \"@/components/ui/AppCard\";\nimport { CardSkeleton } from \"@/components/ui/AppSkeleton\";\nimport { AppEmptyState, AppErrorState } from \"@/components/ui/AppStates\";\nimport { Heading } from \"@/components/ui/heading\";\nimport { IconButton } from \"@/components/ui/IconButton\";\nimport { Input, InputField } from \"@/components/ui/input\";\nimport { Text } from \"@/components/ui/text\";\nimport { VStack } from \"@/components/ui/vstack\";\nimport { useTheme } from \"@/hooks/useTheme\";\nimport { useAuthStore } from \"@/store/authStore\";\nimport { useContributionStore } from \"@/store/contributionStore\";\nimport { selectContributionHasMore } from \"@/store/contributionStore\";\nimport { formatCurrency } from \"@/utils/currency\";\nimport { router } from \"expo-router\";\n\nexport default function ContributionsScreen() {\n  const { colors } = useTheme();\n  const token = useAuthStore((state) => state.token);\n\n  const contributions = useContributionStore((state) => state.contributions);\n  const loading = useContributionStore((state) => state.loading);\n  const error = useContributionStore((state) => state.error);\n  const pagination = useContributionStore((state) => state.pagination);\n  const fetch = useContributionStore((state) => state.fetch);\n  const hasMore = useContributionStore(selectContributionHasMore);\n\n  const [refreshing, setRefreshing] = useState(false);\n  const [searchFilter, setSearchFilter] = useState(\"\");\n\n  // Load on mount\n  useFocusEffect(\n    useCallback(() => {\n      if (token) {\n        fetch(token, 1);\n      }\n    }, [token, fetch])\n  );\n\n  const handleRefresh = async () => {\n    setRefreshing(true);\n    if (token) {\n      await fetch(token, 1);\n    }\n    setRefreshing(false);\n  };\n\n  const handleLoadMore = () => {\n    if (hasMore && token && !loading) {\n      fetch(token, (pagination?.page || 0) + 1);\n    }\n  };\n\n  const filtered = contributions.filter(\n    (c) =>\n      !searchFilter ||\n      c.method?.toLowerCase().includes(searchFilter.toLowerCase()) ||\n      c.reference?.toLowerCase().includes(searchFilter.toLowerCase())\n  );\n\n  const statusColor = (status: string) => {\n    switch (status) {\n      case \"confirmed\":\n        return { bg: colors.success + \"20\", text: colors.success };\n      case \"pending\":\n        return { bg: colors.warning + \"20\", text: colors.warning };\n      default:\n        return { bg: colors.error + \"20\", text: colors.error };\n    }\n  };\n\n  return (\n    <Screen>\n      <VStack className=\"flex-1 gap-4\">\n        {/* Header */}\n        <View className=\"flex-row items-center justify-between\">\n          <Heading size=\"xl\">My Contributions</Heading>\n          <IconButton\n            label=\"Add contribution\"\n            onPress={() => router.push(\"/(tabs)/dashboard\")}\n          >\n            <Plus size={22} color={colors.primary} />\n          </IconButton>\n        </View>\n\n        {/* Search */}\n        <Input variant=\"outline\" size=\"md\">\n          <Search color={colors.muted} size={18} style={{ marginLeft: 8 }} />\n          <InputField\n            placeholder=\"Search by method or reference\"\n            value={searchFilter}\n            onChangeText={setSearchFilter}\n          />\n        </Input>\n\n        {/* Error State */}\n        {error && (\n          <AppErrorState\n            message={error}\n            onRetry={() => {\n              if (token) fetch(token, 1);\n            }}\n          />\n        )}\n\n        {/* Content */}\n        {loading && !contributions.length ? (\n          <VStack className=\"gap-2\">\n            {[1, 2, 3].map((i) => (\n              <CardSkeleton key={i} />\n            ))}\n          </VStack>\n        ) : filtered.length === 0 ? (\n          <AppEmptyState\n            title=\"No contributions\"\n            description={searchFilter ? \"No results found\" : \"Start contributing to your group\"}\n            actionLabel=\"Add Contribution\"\n            onAction={() => router.push(\"/(tabs)/dashboard\")}\n          />\n        ) : (\n          <FlatList\n            data={filtered}\n            keyExtractor={(item) => item.id}\n            scrollEnabled={false}\n            renderItem={({ item }) => {\n              const { bg, text } = statusColor(item.status || \"pending\");\n              return (\n                <AppCard style={{ marginBottom: 8 }}>\n                  <View className=\"flex-row items-center justify-between\">\n                    <VStack className=\"flex-1 gap-1\">\n                      <Text className=\"font-semibold text-base\">\n                        {item.method} •{\" \"}\n                        {formatCurrency(item.amount, item.currency)}\n                      </Text>\n                      <Text className=\"text-xs text-muted-foreground\">\n                        {new Date(item.contributedAt).toLocaleDateString()}\n                      </Text>\n                      {item.reference && (\n                        <Text className=\"text-xs text-muted-foreground\">\n                          Ref: {item.reference}\n                        </Text>\n                      )}\n                    </VStack>\n                    <View\n                      style={{\n                        backgroundColor: bg,\n                        paddingHorizontal: 8,\n                        paddingVertical: 4,\n                        borderRadius: 4,\n                      }}\n                    >\n                      <Text style={{ color: text, fontSize: 11, fontWeight: \"600\" }}>\n                        {item.status === \"confirmed\"\n                          ? \"✓ Confirmed\"\n                          : item.status === \"pending\"\n                          ? \"⏳ Pending\"\n                          : \"✗ \" + item.status}\n                      </Text>\n                    </View>\n                  </View>\n                </AppCard>\n              );\n            }}\n            onEndReached={handleLoadMore}\n            onEndReachedThreshold={0.5}\n            refreshControl={\n              <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} />\n            }\n          />\n        )}\n      </VStack>\n    </Screen>\n  );\n}\n```\n\n---\n\n## 3. Admin Contribution Approvals\n\n**File:** `src/app/admin/contributions.tsx`\n\n```typescript\nimport { AlertCircle, Check, X } from \"lucide-react-native\";\nimport { useCallback, useState } from \"react\";\nimport { FlatList, Pressable, RefreshControl, View } from \"react-native\";\nimport { useFocusEffect } from \"@react-navigation/native\";\nimport { Screen } from \"@/components/layout/Screen\";\nimport { AppCard } from \"@/components/ui/AppCard\";\nimport { CardSkeleton } from \"@/components/ui/AppSkeleton\";\nimport { AppEmptyState, AppErrorState } from \"@/components/ui/AppStates\";\nimport { AppButton } from \"@/components/ui/AppButton\";\nimport { Heading } from \"@/components/ui/heading\";\nimport { Input, InputField } from \"@/components/ui/input\";\nimport { Text } from \"@/components/ui/text\";\nimport { VStack } from \"@/components/ui/vstack\";\nimport { useTheme } from \"@/hooks/useTheme\";\nimport { useAuthStore } from \"@/store/authStore\";\nimport { useContributionStore } from \"@/store/contributionStore\";\nimport { formatCurrency } from \"@/utils/currency\";\nimport type { GroupContribution } from \"@/types/creditManagement\";\n\nexport default function AdminContributionsScreen() {\n  const { colors } = useTheme();\n  const token = useAuthStore((state) => state.token);\n\n  const contributions = useContributionStore(\n    (state) => state.groupContributions\n  );\n  const loading = useContributionStore((state) => state.groupLoading);\n  const mutating = useContributionStore((state) => state.mutating);\n  const error = useContributionStore((state) => state.error);\n  const fetchGroup = useContributionStore((state) => state.fetchGroupContributions);\n  const confirm = useContributionStore((state) => state.confirm);\n  const reject = useContributionStore((state) => state.reject);\n\n  const [refreshing, setRefreshing] = useState(false);\n  const [actionId, setActionId] = useState<string | null>(null);\n  const [rejectReason, setRejectReason] = useState(\"\");\n\n  useFocusEffect(\n    useCallback(() => {\n      if (token) {\n        fetchGroup(token, 1);\n      }\n    }, [token, fetchGroup])\n  );\n\n  const handleRefresh = async () => {\n    setRefreshing(true);\n    if (token) {\n      await fetchGroup(token, 1);\n    }\n    setRefreshing(false);\n  };\n\n  const handleConfirm = async (id: string) => {\n    if (!token) return;\n    try {\n      await confirm(token, id);\n    } catch {}\n    setActionId(null);\n  };\n\n  const handleReject = async (id: string) => {\n    if (!token) return;\n    try {\n      await reject(token, id, rejectReason);\n    } catch {}\n    setActionId(null);\n    setRejectReason(\"\");\n  };\n\n  const pending = contributions.filter((c) => c.status === \"pending\");\n\n  return (\n    <Screen>\n      <VStack className=\"flex-1 gap-4\">\n        <Heading size=\"xl\">Contribution Approvals</Heading>\n\n        {error && (\n          <AppErrorState\n            message={error}\n            onRetry={() => {\n              if (token) fetchGroup(token, 1);\n            }}\n          />\n        )}\n\n        {loading && !contributions.length ? (\n          <VStack className=\"gap-2\">\n            {[1, 2, 3].map((i) => (\n              <CardSkeleton key={i} />\n            ))}\n          </VStack>\n        ) : pending.length === 0 ? (\n          <AppEmptyState\n            title=\"No pending approvals\"\n            description=\"All contributions have been reviewed\"\n          />\n        ) : (\n          <FlatList\n            data={pending}\n            keyExtractor={(item) => item.id}\n            scrollEnabled={false}\n            renderItem={({ item }) => (\n              <AppCard key={item.id} style={{ marginBottom: 12 }}>\n                <VStack className=\"gap-3\">\n                  {/* Member Info */}\n                  <View>\n                    <Text className=\"font-bold text-base\">\n                      {item.member?.name || \"Unknown Member\"}\n                    </Text>\n                    <Text className=\"text-xs text-muted-foreground\">\n                      {item.member?.memberNumber || \"\"}\n                    </Text>\n                  </View>\n\n                  {/* Contribution Details */}\n                  <View className=\"gap-2 bg-muted/10 p-3 rounded\">\n                    <View className=\"flex-row justify-between\">\n                      <Text className=\"text-sm text-muted-foreground\">\n                        Amount\n                      </Text>\n                      <Text className=\"font-semibold\">\n                        {formatCurrency(item.amount, item.currency)}\n                      </Text>\n                    </View>\n                    <View className=\"flex-row justify-between\">\n                      <Text className=\"text-sm text-muted-foreground\">\n                        Method\n                      </Text>\n                      <Text className=\"font-semibold\">{item.method}</Text>\n                    </View>\n                    <View className=\"flex-row justify-between\">\n                      <Text className=\"text-sm text-muted-foreground\">\n                        Date\n                      </Text>\n                      <Text className=\"font-semibold\">\n                        {new Date(item.contributedAt).toLocaleDateString()}\n                      </Text>\n                    </View>\n                    {item.reference && (\n                      <View className=\"flex-row justify-between\">\n                        <Text className=\"text-sm text-muted-foreground\">\n                          Reference\n                        </Text>\n                        <Text className=\"font-semibold text-xs\">\n                          {item.reference}\n                        </Text>\n                      </View>\n                    )}\n                  </View>\n\n                  {/* Actions */}\n                  <View className=\"flex-row gap-2\">\n                    <AppButton\n                      title={mutating && actionId === item.id ? \"Confirming...\" : \"Confirm\"}\n                      loading={mutating && actionId === item.id}\n                      disabled={mutating}\n                      onPress={() => {\n                        setActionId(item.id);\n                        handleConfirm(item.id);\n                      }}\n                      style={{ flex: 1 }}\n                    />\n                    <AppButton\n                      title=\"Reject\"\n                      variant=\"outline\"\n                      disabled={mutating}\n                      onPress={() => {\n                        if (actionId === item.id) {\n                          handleReject(item.id);\n                        } else {\n                          setActionId(item.id);\n                        }\n                      }}\n                      style={{ flex: 1 }}\n                    />\n                  </View>\n\n                  {/* Reject Reason Input */}\n                  {actionId === item.id && (\n                    <Input variant=\"outline\" size=\"sm\">\n                      <InputField\n                        placeholder=\"Reason for rejection (optional)\"\n                        value={rejectReason}\n                        onChangeText={setRejectReason}\n                        multiline\n                      />\n                    </Input>\n                  )}\n                </VStack>\n              </AppCard>\n            )}\n            refreshControl={\n              <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} />\n            }\n          />\n        )}\n      </VStack>\n    </Screen>\n  );\n}\n```\n\n---\n\n## Implementation Notes\n\n1. **Always import from correct locations** - Use dashboardApi, not contributionApi for new endpoints\n2. **Validate against settings** - Check minimumAmount, allowedMethods, etc. before submission\n3. **Handle loading/error states** - Use proper skeleton loaders and error displays\n4. **Use formatCurrency** - Always format amounts with the currency from backend responses\n5. **Fetch settings when needed** - Modal should load settings before displaying form\n6. **Refresh after actions** - After confirm/reject/add, refresh the list\n7. **Proper TypeScript types** - Use types from creditManagement.ts\n8. **Admin protection** - These screens should check user.role === \"admin\"\n\nThese templates can be used as-is or customized to match your specific design system and requirements.\n